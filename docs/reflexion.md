# Reflexión EC1 F1 A2
**Nombre:** Jonathan Aaron Rivera Nevarez
**Grupo:** Ingeniería en Software

## 1. Función de Node.js
Proporciona el entorno de ejecución para correr código JavaScript fuera del navegador en el servidor.

## 2. Aportes de TypeScript
Añade tipado estático para validar la estructura de los datos e identificar errores antes de ejecutar el programa.

## 3. Separación por módulos
Organiza la arquitectura del proyecto separando modelos, datos en memoria, reglas de negocio y utilidades reutilizables.

## 4. Función síncrona vs async
Una función síncrona se ejecuta secuencialmente bloqueando el hilo, mientras que una función `async` maneja operaciones asíncronas retornando promesas sin detener el programa[cite: 1].

## 5. Retorno Task | undefined
Porque la búsqueda por ID en una colección puede no encontrar coincidencias[cite: 1].

## 6. Ventaja de process.env
Permite modificar parámetros de configuración externamente sin alterar ni rebalancear el código fuente[cite: 1].

## 7. pnpm start vs pnpm build + serve
`pnpm start` ejecuta TypeScript directamente en desarrollo con `tsx`, mientras que `pnpm build + serve` compila a JavaScript puro en `dist/` y lo corre nativamente con Node.js[cite: 1].

## 8. Reutilización con Express
Las interfaces, datos y servicios creados aquí se mantendrán intactos para conectarse directamente a las rutas HTTP de Express[cite: 1].
